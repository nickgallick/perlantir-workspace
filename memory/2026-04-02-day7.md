# 2026-04-02 — Day 7: SDK Ergonomics + E2E + Demo Polish

## What Happened

Day 7 approved at 06:03 UTC+8. Completed at 06:10 UTC+8.

### SDK Enhancements
- Added `NexusApiError` class that preserves the server's error envelope (`{ error: { code, message, details? } }`)
- Previously SDK threw generic `Error` with stringified text — now consumers can inspect `.status`, `.code`, `.serverMessage`, `.details`
- Added missing methods to match 100% of server routes:
  - `updateDecisionStatus(id, status, opts?)` → PATCH /api/decisions/:id
  - `createEdge(sourceId, input)` → POST /api/decisions/:id/edges
  - `listEdges(decisionId)` → GET /api/decisions/:id/edges
  - `deleteEdge(edgeId)` → DELETE /api/edges/:id
  - `listArtifacts(projectId)` → GET /api/projects/:id/artifacts
  - `health()` → GET /api/health
- Fixed `getDecisionGraph` return type from `unknown[]` to `ConnectedDecision[]`
- Added `HealthResponse`, `NexusErrorEnvelope` types

### SDK E2E Tests (27 new)
- Routes through real Hono app via `globalThis.fetch` interception
- No HTTP server needed, but tests real DB operations
- Covers: health, errors (3), projects, agents, decisions (5), edges (4), artifacts (2), graph, compile (3), role differentiation (2), notifications (2), full lifecycle (1)

### Demo Script
- Rewritten with 4 sections: A (baseline), B (Nexus per-role), C (change propagation), D (SDK ergonomics)
- Added: health check on startup, artifact registration, edge/graph/error handling showcase
- Requires server running — developer path documented in script header

## Test Counts
- Core: 150 (8 files) — unchanged
- SDK: 36 (3 files) — +27 E2E
- Server: 27 (2 files) — unchanged
- Total: 213/213 pass, 13 test files

## Files Changed
- `packages/sdk/src/client.ts` — enhanced
- `packages/sdk/src/index.ts` — new exports
- `packages/sdk/tests/e2e.test.ts` — new
- `packages/sdk/package.json` — added dev deps
- `packages/sdk/vitest.config.ts` — test config
- `examples/software-team/comparison.ts` — rewritten
